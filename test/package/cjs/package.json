{
  "name": "test-cjs-package",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf target && rm -f yarn*",
    "test": "webpack"
  },
  "dependencies": {
    "@okta/okta-signin-widget": "file:../../../dist"
  },
  "devDependencies": {
    "source-map-loader": "^2.0.2",
    "webpack": "^5.91.0",
    "webpack-bundle-analyzer": "^4.5.0",
    "webpack-cli": "^4.8.0"
  }
}
