define(['shared/views/profile/forms/inputs/DeletableBox',
        'shared/util/StringUtil'
], function (DeletableBox, StringUtil) {
  return DeletableBox.extend({
    className: 'o-form-input-group o-form-input-group-compact',

    // @Override
    getPlaceholderText: function () {
      return StringUtil.localize('idp.config.oidc.settings.enterAUri');
    }
  });
});