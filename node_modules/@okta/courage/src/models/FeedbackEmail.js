define(['shared/models/BaseModel'],
function (BaseModel) {
  return BaseModel.extend({
    urlRoot: '/api/internal/self-service/feedback',
    defaults: {
      feedback: '',
      subject: ''
    }
  });
});