define([
  'shared/models/BaseCollection', 'shared/models/BaseModel'
], function (BaseCollection, BaseModel) {
  var AppInstanceProperty = BaseModel.extend({
    defaults: {
      internalName: null,
      name: null,
      label: null,
      type: 'STRING',
      helpText: null,
      hidden: false,
      required: false,
      pattern: null,
      updatable: true,
      displayOrder: 0,
      referenceTypeName: null,
      enumTypeName: null,
      setting: 'GENERAL'
    }
  });

  var AppInstanceProperties = BaseCollection.extend({
    model: AppInstanceProperty
  });

  return {
    Model: AppInstanceProperty,
    Collection: AppInstanceProperties
  };
});